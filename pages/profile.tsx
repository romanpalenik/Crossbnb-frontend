import { NextPage } from "next";
const CreateOffer: NextPage = () => {
  return <div></div>;
};

//CANNOT use getServerSideProps because of account is set after rendering a page
export default CreateOffer;
